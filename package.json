{
  "name": "amazing-aaryan-portfolio",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/fetch-paper-assets.mjs && node scripts/build-static.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
